export enum SeverityLevel {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
}

export enum ResolutionStatus {
  UNRESOLVED = 'unresolved',
  RESOLVED = 'resolved',
  IN_PROGRESS = 'in_progress',
}
